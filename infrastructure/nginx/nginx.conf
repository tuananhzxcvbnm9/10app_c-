events {}
http {
  include /etc/nginx/mime.types;
  limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;
  server {
    listen 80;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Content-Security-Policy "default-src 'self' https: data: blob: 'unsafe-inline' 'unsafe-eval'" always;

    location / {
      return 302 /app-01/;
    }

    location /app-01/ {
      proxy_pass http://app-01-web:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
    location = /app-01 { return 301 /app-01/; }
    location /api/app-01/ {
      limit_req zone=api_limit burst=60 nodelay;
      proxy_pass http://app-01-api:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Correlation-ID $request_id;
    }

    location /app-02/ {
      proxy_pass http://app-02-web:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
    location = /app-02 { return 301 /app-02/; }
    location /api/app-02/ {
      limit_req zone=api_limit burst=60 nodelay;
      proxy_pass http://app-02-api:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Correlation-ID $request_id;
    }

    location /app-03/ {
      proxy_pass http://app-03-web:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
    location = /app-03 { return 301 /app-03/; }
    location /api/app-03/ {
      limit_req zone=api_limit burst=60 nodelay;
      proxy_pass http://app-03-api:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Correlation-ID $request_id;
    }

    location /app-04/ {
      proxy_pass http://app-04-web:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
    location = /app-04 { return 301 /app-04/; }
    location /api/app-04/ {
      limit_req zone=api_limit burst=60 nodelay;
      proxy_pass http://app-04-api:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Correlation-ID $request_id;
    }

    location /app-05/ {
      proxy_pass http://app-05-web:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
    location = /app-05 { return 301 /app-05/; }
    location /api/app-05/ {
      limit_req zone=api_limit burst=60 nodelay;
      proxy_pass http://app-05-api:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Correlation-ID $request_id;
    }

    location /app-06/ {
      proxy_pass http://app-06-web:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
    location = /app-06 { return 301 /app-06/; }
    location /api/app-06/ {
      limit_req zone=api_limit burst=60 nodelay;
      proxy_pass http://app-06-api:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Correlation-ID $request_id;
    }

    location /app-07/ {
      proxy_pass http://app-07-web:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
    location = /app-07 { return 301 /app-07/; }
    location /api/app-07/ {
      limit_req zone=api_limit burst=60 nodelay;
      proxy_pass http://app-07-api:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Correlation-ID $request_id;
    }

    location /app-08/ {
      proxy_pass http://app-08-web:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
    location = /app-08 { return 301 /app-08/; }
    location /api/app-08/ {
      limit_req zone=api_limit burst=60 nodelay;
      proxy_pass http://app-08-api:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Correlation-ID $request_id;
    }

    location /app-09/ {
      proxy_pass http://app-09-web:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
    location = /app-09 { return 301 /app-09/; }
    location /api/app-09/ {
      limit_req zone=api_limit burst=60 nodelay;
      proxy_pass http://app-09-api:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Correlation-ID $request_id;
    }

    location /app-10/ {
      proxy_pass http://app-10-web:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
    location = /app-10 { return 301 /app-10/; }
    location /api/app-10/ {
      limit_req zone=api_limit burst=60 nodelay;
      proxy_pass http://app-10-api:8080/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Correlation-ID $request_id;
    }

  }
}
